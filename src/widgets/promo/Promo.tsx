import React from 'react';

const Promo = () => {
    return (
        <div className={`w-full h-[100vh] bg-background z-10 flex items-center justify-center`}>
            <p className={`text-[50px] font-railway font-semibold`}>Проморолик</p>
        </div>
    );
};

export default Promo;